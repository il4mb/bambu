type SelectorProps = {
    
};

export default function Selector({}: SelectorProps) {
    return (
        <div>
            {/* Selector content goes here */}
            <h1>Selector</h1>
        </div>
    );
}
