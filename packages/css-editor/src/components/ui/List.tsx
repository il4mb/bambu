import styled from "@emotion/styled";

const List = styled.ul({
    color: "#000",
    listStyle: "none",
    fontSize: 12,
    fontFamily: "monospace",
    maxHeight: 200,
    overflow: "auto",
});

export default List;